SAVER MARKET SEO PRODUCT PAGE FIX V3

This package fixes the blank /p/SKU product pages on Cloudflare Pages.

Merge these files into the ROOT of your existing GitHub repository:

index.html                      -> replace existing index.html
robots.txt                      -> replace existing robots.txt
sitemap.xml                     -> replace existing sitemap.xml
functions/sitemap-products.js   -> replace existing file
functions/p/[sku].js            -> IMPORTANT: replace existing file

Do NOT delete your other existing website files.
Keep the folder structure exactly as shown.
GitHub-connected Cloudflare Pages should deploy automatically after commit.

After deployment test:
https://savermarketshop.com/p/STK132
